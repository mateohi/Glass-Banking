<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uy.infocorp.banking.glass.icbankingglass" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".controller.exchange.ExchangeRateMenuActivity"
            android:theme="@style/MenuTheme" />

        <service
            android:name=".controller.exchange.ExchangeRateService"
            android:icon="@drawable/ic_glass_logo"
            android:label="@string/title_activity_exchange_rate_service" >
            <intent-filter>
                <action android:name="com.google.android.glass.action.VOICE_TRIGGER" />
            </intent-filter>

            <meta-data
                android:name="com.google.android.glass.VoiceTrigger"
                android:resource="@xml/voice_trigger" />
        </service>
    </application>

</manifest>

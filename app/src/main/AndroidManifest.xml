<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uy.infocorp.banking.glass" >

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="com.google.android.glass.permission.DEVELOPMENT" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_infocorp"
        android:label="@string/app_name" >

        <!-- Exchange rates -->
        <activity
            android:name="uy.infocorp.banking.glass.controller.exchange.ExchangeRateMenuActivity" />

        <service
            android:name="uy.infocorp.banking.glass.controller.exchange.ExchangeRateService"
            android:icon="@drawable/ic_infocorp"
            android:label="@string/title_activity_exchange_rate_service" >
            <intent-filter>
                <action android:name="com.google.android.glass.action.VOICE_TRIGGER" />
            </intent-filter>

            <meta-data
                android:name="com.google.android.glass.VoiceTrigger"
                android:resource="@xml/exchange_rate_voice_trigger" />
        </service>
        <!-- /Exchange rates -->

        <!-- Benefits -->
        <activity
            android:name="uy.infocorp.banking.glass.controller.benefit.BenefitsMenuActivity" />

        <service
            android:name="uy.infocorp.banking.glass.controller.benefit.BenefitsService"
            android:icon="@drawable/ic_infocorp"
            android:label="@string/show_me_benefits"
            android:enabled="true" >
            <intent-filter>
                <action android:name="com.google.android.glass.action.VOICE_TRIGGER" />
            </intent-filter>
            <meta-data
                android:name="com.google.android.glass.VoiceTrigger"
                android:resource="@xml/benefits_voice_trigger" />
        </service>
        <!-- Benefits -->
    </application>

</manifest>
